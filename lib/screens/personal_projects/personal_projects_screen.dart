import 'package:flutter/material.dart';

class PersonalProjectsScreen extends StatelessWidget {
  const PersonalProjectsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Personal Projects");
  }
}
